fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5
python-dotenv==1.0.1
pandas==2.2.3
# MetaTrader5 5.0.45 is built against the numpy 1.x C ABI —
# numpy 2.x fails with "numpy.core.multiarray failed to import".
numpy==1.26.4
google-cloud-firestore==2.19.0
MetaTrader5==5.0.45
requests==2.34.2

# --- ADK agent fleet (adk_agents/) ---
# Google Agent Development Kit + Gemini API. This is what drives the
# Orchestrator and the five specialist agents against the backend above.
google-adk==2.8.0
